# 遗留问题清单

> 生成日期：2026-09-09 · 基线：dev 分支（超时链路修复后工作区）
> 行号为当日代码快照，后续改动可能偏移；标注 ⚠️ 的条目行号未重新核对。
> 来源：全项目审计（后端安全 / 后端性能 / 前端 / 算法服务与架构）+ 修复过程中新发现。

## 已修复（对照，勿重复报告）

| # | 问题 | 修复提交/位置 |
|---|---|---|
| 1 | 评价查询无参全表重算（自我 DoS）+ 学生/教师越权读评价 | `GET /evaluations`、`/evaluations/results` 强制作用域（缺参 422），学生收敛本人、教师跨课 403，删除全表重算分支；`backend/tests/test_evaluations_access.py` 回归 |
| 2 | 旧库缺 `exam_batch.exam_time` 列导致评价实时重算 500 | 启动迁移补列并回填 `create_time`（`app/core/database.py` `_migrate_legacy_tables`） |
| 3 | 跨服务超时链路倒挂 + 8001 地址硬编码 5 处 | 新增 `app/core/ai_client.py`；超时全部配置化（`AI_JUDGE/REPORT/GENERATE/EXAM_TOOL_TIMEOUT`）；算法侧判分/报告/Agent 单步改为单次调用不重试，判分低温度短输出 |

---

## P0 致命（建议本周内修）

### 安全：一条完整攻击链（匿名枚举 → 弱口令 → 越权拖库）

- [x] **A1. 学生 PII 接口完全无鉴权**
  - 位置：`backend/app/api/v1/students.py:18`（router 无依赖）、`students.py:36`（`GET /students`）、`students.py:114`（`GET /students/{id}`）；`main.py:71` 直接挂载
  - 问题：未登录可拉取全班姓名/学号/手机号/邮箱（`_serialize_student` 全字段返回）
  - 修复：router 加 `Depends(get_current_user)`；学生只允许查自己；手机号/邮箱脱敏
- [x] **A2. 字典/课程接口未认证**（攻击链侦察跳板）
  - 位置：`backend/app/api/v1/dicts.py:8`（7 个端点仅 `Depends(get_session)`，`/classes/{id}/students` 返回学号+姓名）、`courses.py:13`（`GET /courses`、`GET /courses/{id}`）
  - 修复：至少要求登录；班级学生名单限教师/管理员
- [x] **A3. 学生越权读任意学生成绩与考勤（IDOR）**
  - 位置：`backend/app/api/v1/scores.py:12` + `scores.py:101`（`/score-records/student/{student_id}`）、`scores.py:15`（`/score-records`）、`attendance.py:9`
  - 问题：`require_teaching_user = require_roles("teacher", "student")`（`permissions.py:51`）含学生角色，端点内无归属校验，传任意 `student_id` 可读全校成绩/考勤
  - 修复：`permissions.py` 新增 `resolve_student_scope`，scores/attendance 三个读端点接入——学生角色强制收敛为本人（缺省自动注入本人 id，传他人 id 403，未绑定档案 403）；教师/管理员不限制；`backend/tests/test_scores_attendance_access.py` 回归。教师按授课课程过滤暂缓（端点无课程归属上下文，需另行设计）
- [ ] **A4. 弱密码 123456 全链路**
  - 位置：`backend/app/seed.py`（84 处，含 admin/教师/47 测试学生）；`frontend/src/api/users.ts:94`（**管理员建号固定写死 password=123456**）；`frontend/src/views/system/UserManageView.vue:231`（重置密码也固定 123456）；`frontend/src/views/login/LoginView.vue:26-44,135`（登录页公示全部演示账号+密码，已打进 dist）
  - 影响：即使改 seed，新建账号仍是弱口令；登录页泄露 admin 账号名
  - 修复：建号/重置接口加密码输入或随机生成+首登强制改密；演示账号入口用 `import.meta.env.DEV` 门控；seed 仅限开发
- [ ] **A5. SECRET_KEY 缺省回退到源码固定值**
  - 位置：`backend/app/core/config.py:61`
  - 问题：`ENVIRONMENT` 忘配 production 时使用仓库公开密钥，可离线伪造任意用户 JWT（含 admin）
  - 修复：删除固定回退串；dev/test 用 `secrets.token_urlsafe` 持久化到本地文件或启动 fail-fast

### 正确性：功能坏 / 数据丢失 / 自我 DoS

- [ ] **B1. Agent 智能组卷必然 422（从未可用）**
  - 位置：`backend/app/services/agent/tools/exam.py:81`（发 `"question_types": []`）；`algorithm/src/schemas.py:35`（要求 `QuestionTypeDistribution` 对象）
  - 修复：按 `total_count` 构造题型分布对象；补一条真实 payload 契约的 TestClient 测试（现全部走 MockLLMProxy，测不出来）
- [ ] **B2. 交卷判分持有 SQLite 写锁期间串行调 LLM**
  - 位置：`backend/app/api/v1/quiz.py:633`（`flush()` 删旧记录取得写锁）→ `quiz.py:646`（循环内逐题 `_call_ai_judge`，单题 150s）→ `quiz.py:677`（同事务 `refresh_student_analysis`）
  - 影响：N 道简答题作业持写锁最长 N×150s，期间全站写请求 `database is locked`；占死线程池槽位。**全站可用性第一风险**
  - 修复：删旧记录挪到全部判分完成后；判分结果收集完在一个短事务内落库；AI 调用有界信号量并发化
- [ ] **B3. 评价落库非原子写（数据丢失窗口）**
  - 位置：`backend/app/services/evaluation.py:570`（删旧+commit）→ `:583`（插总分+commit）→ `:594`（插维度分+commit）
  - 影响：中间任何失败/进程被杀 → 该学生评价被永久清空；被后台刷新链路批量调用
  - 修复：合并单事务（末尾一次 commit）或改 UPSERT
- [ ] **B4. 算法服务（8001）零鉴权 + 一键脚本绑 0.0.0.0**
  - 位置：`algorithm/src/main.py:41-56`（4 个业务路由无任何 API-Key）；`start-project.bat`（`--host 0.0.0.0`）
  - 影响：局域网任意主机可白嫖付费 LLM Key（`/agent/chat` 接受任意 messages/tools）、注入 prompt；`/health` 暴露 provider/model
  - 修复：共享密钥头（backend 配 `AI_SERVICE_KEY`，algorithm 侧校验 `X-Internal-Key`）；绑定改 `127.0.0.1`；`/agent/chat` 加 messages 长度/轮数上限

---

## P1 高（两周内）

### 后端安全
- [ ] **C1. 登录无速率限制/失败锁定**（`auth.py:74-93` `/login`、`/assistant/login`；配合 A1 学号枚举 + A4 弱口令极危险）→ slowapi 或 redis 计数，IP+账号双维度，5 次锁定
- [ ] **C2. token 7 天有效，改密不吊销旧会话**（`auth.py` exp、`operation_log.py` 验签无 `iat` 下限；管理员重置他人密码同样不吊销）→ `SysUser.password_changed_at` + `iat` 对比；token 降到小时级 + refresh
- [ ] **C3. 旧明文密码兼容回退**（`security.py` `hmac.compare_digest(password, stored_password)` 分支）→ 一次性迁移强制重置非 scrypt 记录，删明文分支
- [ ] **C4. 审计日志 IP 取自可伪造的 X-Forwarded-For**（`operation_log.py:45-51`）→ 仅在可信代理后信任该头

### 后端性能/稳定性
- [ ] **D1. SQLite 无 WAL/busy_timeout**（`database.py:16-18` 仅 `check_same_thread=False`）→ 连接事件 `PRAGMA journal_mode=WAL; busy_timeout=5000; foreign_keys=ON`。与 B2 叠加放大，改动小收益大
- [ ] **D2. 画像重算 O(N²)**（`analysis.py:389` `_get_class_profile` 逐学生 `compute_profile` 不传 `class_slopes`，每人重算全班斜率；`profile.py:514-525` 内部重复 2×；`analysis_refresh.py:213-272` 同一学生跑 2-3 次）→ 预计算一次显式传入
- [ ] **D3. Agent 会话表无限累积 + 无锁**（`memory.py:176` `_sessions` 只增不删；线程池多线程读写无锁）→ LRU 上限 + TTL + `threading.Lock`；`persist_session` 后驱逐
- [ ] **D4. SSE 生成器缺 try/finally**（`agent/base.py:371-482` `run_agent_stream`：客户端断开触发 `GeneratorExit` 时不持久化会话也不关 `session_factory()` 连接；`quiz.py` 流式出题生成器同样）→ 生成器体包 `try/finally`
- [ ] **D5. 同步 LLM 调用无并发上限/重试**（`llm_proxy.py`、`judge/quiz/report` httpx.post；默认线程池 40 槽，10 个并发 Agent 请求可拖垮全站）→ 全局信号量 + 指数退避；`httpx.Client` 连接复用
- [ ] **D6. embedding 异常被 lru_cache 永久缓存**（`core/embedding.py` 缓存 `(向量, 异常)` 元组，一次抖动固化为永久失败）→ 只缓存成功结果
- [ ] **D7. 预警扫描 O(学生²)**（`services/warning.py:150-162`、`:242-257` 逐条目重查全班）→ 进入 `scan_course_warnings` 前预载「批次→全班成绩」「任务→全班提交」字典
- [ ] **D8. vector_admin 同步端点内逐条串行嵌入**（`vector_admin.py:32-81`，500 题课程占线程池数分钟）→ 后台任务 + 进度查询；嵌入按批
- [ ] **D9. 列表接口 N+1/无分页集中区**（⚠️ 行号未复核）：`teaching_data.py:129-440`（无 keyword 全表 + Python 侧分页，page_size 上限 10000）、`scores.py`（逐行 `session.get(ExamBatch/Course)`）、`question_bank.py:141-182`、`analysis.py:553-555,793-823,984-1007`、`students.py:60-65`、`dashboard.py:623-654,220-223` → 统一 `in_` 批量映射 + SQL 分页

### 前端
- [ ] **E1. SSE 流无超时无取消**（`api/agent.ts:57-71` fetch 无 `signal`；`AgentChat.vue:152-194`、`DiagnosisView.vue:383-453` 卸载不中断；全项目 0 处 AbortController）→ `streamAgentChat` 接受 `AbortSignal`，`onBeforeUnmount` abort + 300s 兜底超时
- [ ] **E2. 全局零竞态防护**（0 处防抖/序号/abort）：`QuestionBankView.vue`、`DataManageView.vue:250-261`、`WarningView.vue:137-140`、`StudentDashboard.vue:174-176`、`QuizAnswerView.vue:124-137`、`useDashboardFilter.ts:199-266` → 封装 `useLatest(fn)`（递增 requestId 赋值前比对），参照 `useAnalysisScope.ts` 的重入锁
- [ ] **E3. 身份缺失兜底到他人 ID**（`QuizResultView.vue:109` `studentId || 1`、`QuizAnswerView.vue:207` `classId || 0`）→ 缺失时提示并 return
- [ ] **E4. 403 全局静默 + 误导文案**（`request.ts:85-86`；叠加 `DataImportView.vue:199-200` 权限失败显示"文件格式错误"）→ 一次性提示"无权操作"
- [ ] **E5. token/角色存 localStorage 且恢复不校验**（`utils/auth.ts`、`stores/user.ts:66-76` 只 JSON.parse；用户手改 role=admin 可绕过全部前端守卫）→ 数据安全靠后端兜底（P0 已列）；中期改 HttpOnly Cookie + `/me` 恢复

### 算法服务
- [ ] **F1. 题型枚举不一致，按多选过滤静默丢题**（`rag_service.py:27` 用 `multi_choice`，`agent/tools/exam.py:184` `_TYPE_MAP` 用 `multiple_choice`）→ backend 定义单一映射常量复用
- [ ] **F2. 算法服务零自动化测试**（无 tests 目录；`test_algorithm.py` 测的是 backend 算法不是 8001 服务；pytest 不在任何 requirements）→ TestClient 契约测试（mock OpenAI SDK），覆盖 4 路由请求/响应模型与降级路径——B1 这类坏契约只有它能拦住

---

## P2 中

- [ ] **G1. AI 判分不可达时学生简答题直接落 0 分**（`quiz.py:762-775` `except (httpx.HTTPError, ValueError)` 写 `score=0`，仅 `judge_reason` 标记）→ 增加待人工状态/任务表，不落确定性 0 分（污染 D05 掌握度）
- [ ] **G2. judge 判分无 UPSERT/唯一约束 + 阈值硬编码**（`judge.py:91-116` 每次新插 `StudentAnswerRecord`；启动迁移已按组去重说明线上出现过重复行；`is_correct = score >= 6.0` 假设满分 10）→ 唯一约束 `(task_id, student_id, question_id)` + UPSERT；按题目满分归一化
- [ ] **G3. AI 配额先查后插竞态 + naive datetime**（`quiz.py` `_consume_ai_quota`；`task.deadline < datetime.now()`）→ 唯一约束 + 冲突回滚判定；统一 UTC/带时区
- [ ] **G4. eval_config 后台 threading.Timer 直写 SQLite**（`eval_config.py:43-72` 与 API 线程争锁，进程退出可能半写）→ 进程内单写者任务队列
- [ ] **G5. report 历史快照 json.loads 无防护 + 错误细节外泄**（`report.py` `_history_to_dict`；LLM 失败 `str(e)[:200]` 进响应体）→ 逐字段 try/except；错误只记日志
- [ ] **G6. predict/mastery 数值健壮性**（`predict.py:116,132,148` `float(score)` 无 None 防护；`mastery.py:314` `if ... or True:` 恒真死分支导致每次全量加载）→ None 归零/查询层过滤；删恒真分支
- [ ] **G7. 文件导入逐行 4-6 查询 + 行内多次 commit**（`services/file_import.py` 440-826 区间）→ 预载缓存字典，整 Sheet 一次提交
- [ ] **G8. 上传先整体读进内存再查 25MB 上限**（`teaching_data.py` `file.file.read()`）→ 分块读或先校验 Content-Length
- [ ] **G9. 密码策略过弱**（改密仅 `len >= 6`；管理员建号无强度校验）→ ≥8 位 + 复杂度/黑名单，建号改密共用校验器
- [ ] **G10. Excel 导出公式注入**（`logs.py:137-149`、`report.py` `_write_sheet`：`=`/`+`/`-`/`@` 开头写入成公式）→ 前缀净化或 `data_type='s'`
- [ ] **G11. vector_admin 不校验课程归属**（`vector_admin.py:32-81` 教师 A 可重建教师 B 课程乃至全库向量）→ 循环内按 `course.teacher_id` 校验；`courseId=0` 仅 admin
- [ ] **G12. 8001 `/health` 信息泄露**（`algorithm/src/main.py:59-68` 返回 provider/model，配合 B4）
- [ ] **G13. 前端打包体积**（Element Plus/ECharts/XLSX 全量引入，主 chunk 1.22MB、总 3.5MB；mock 代码进生产 bundle）→ `unplugin-vue-components` 按需、`echarts/core` 按需注册、xlsx 动态 import、`USE_MOCK` 改读 `import.meta.env`（`request.ts:16` 现硬编码 false，`.env.development` 的 `VITE_USE_MOCK=true` 是摆设）
- [ ] **G14. 前端权限/标题三处重复定义**（`router/index.ts` meta.roles、`config/menu.ts` roles + routeTitleMap）→ 路由表单一来源生成
- [ ] **G15. deptId:1、演示学期等单学院假设散落 10+ 处**（`useAnalysisScope.ts`、`useDashboardFilter.ts:298`、`ReportCenterView.vue:107-109` 等）→ 收敛到 `src/config/constants.ts` 或后端下发
- [ ] **G16. `algorithm/.env` 含 2 个历史废弃 API Key 明文注释**（未入库但建议删除 + 轮换现用 Key）
- [ ] **G17. dashboard 看板假数据**（`DashboardView.vue:120-123` `passRate * (0.95 + f*0.05)` 修饰系数 + 写死趋势 3.2/1.8/-12）→ 删乘数；趋势接后端环比或去掉

## P3 低 / 工程卫生

- [ ] **H1. `start-project.bat` 开箱即坏**：检查根目录 `.venv`（不存在，实际在 `backend/.venv`）；README 从未提及该脚本 → 修路径或删除，与 README 统一口径
- [ ] **H2. 虚拟环境堆积**：`backend/.venv-1/2/3`、`algorithm/.venv-1`（各 59MB）→ 清理；`.gitignore` 加 `*.venv*`
- [ ] **H3. `backend/chroma_data/` 目录未整目录 ignore**（目前靠 `*.sqlite3` 规则碰巧没入库；Chroma 的 `-wal/-shm` 文件会漏进来）→ `.gitignore` 加 `chroma_data/`
- [ ] **H4. 本地残留文件**：`backend/app_test_copy.db`（14MB×3 个 db 文件）；`backend/package-lock.json`（前端产物误放）→ 清理
- [ ] **H5. README 与代码矛盾**：默认厂商写 DeepSeek（实际 qwen/智谱）；建议 `LLM_TIMEOUT=30`（必超时）；目录树称 algorithm「占位」→ 同步
- [ ] **H6. docs 两份功能清单互相矛盾且过时**（`待完成功能缺口清单_v2.0.md` 的 P1-1/P1-3 已实现，`已完成功能点清单.md` 停在 7 月）→ 合并为单一功能状态矩阵 + 核对日期
- [ ] **H7. 测试顺序依赖**（新发现）：`test_algorithm.py::TestWarning::test_class_refresh_preserves_other_class_warnings` 全量运行时因共享内存库残留 `sys_user` 触发 UNIQUE 冲突，单独跑通过（stash 对照确认与近期改动无关）→ 测试内自建数据用唯一 ID 或 fixture 隔离
- [ ] **H8. 死代码**：`agent/base.py:290` `tool_calls=[...] if False else []`（assistant 工具上下文永不入历史）；`stores/app.ts` pageLoading、`stores/user.ts` hasRole 零引用；`api/agent.ts:20` courseName 参数未使用
- [ ] **H9. 禁用账号可被探测**（`auth.py:87-88` 密码正确但禁用返回 403 与 401 可区分）→ 统一 401
- [ ] **H10. 生产建议项**：`main.py` 关闭 `/docs`/`/redoc`；上传按扩展名放行未验魔数（已有 25MB 上限 + uuid 文件名缓解）；`md5` 缓存键换 sha256；ESLint 用 flat/essential 而非 recommended
- [ ] **H11. Chroma 并发写约束**（`vector_store.py:44-47` 单实例安全；`--reload` 或多副本同目录无文件锁）→ 部署文档明确单实例约束
- [ ] **H12. 报告增强请求契约靠 pydantic 忽略多余字段**（`report.py` 传 `report_type_name` 被 algorithm 侧丢弃，回退通用报告）→ algorithm 侧显式声明字段 + `extra="forbid"`
- [ ] **H13. AppHeader 通知 30s 轮询**页面隐藏时照发（`AppHeader.vue:64`）→ 加 `document.visibilityState` 判断

---

## 建议修复顺序

1. **一个 PR 拆掉安全攻击链**（A1+A2+A3+A4 后端部分，半天）：students/dicts/courses 加鉴权 + scores/attendance 本人过滤 + 建号密码自定义
2. **B2 + D1**（半天，可用性收益最大）：SQLite WAL/busy_timeout + 判分 AI 调用移出写锁
3. **B1 + F2**（一天）：修组卷 payload + 算法服务契约测试（防回归）
4. **B3 + B4**（一天）：评价落库单事务 + 8001 共享密钥 + 绑 127.0.0.1
5. **A5 + C1 + C2**（一天）：SECRET_KEY、登录限流、会话吊销
6. 其余按 P1 → P2 → P3 排期；D3/D4（内存与连接泄漏）建议随任意后端 PR 顺带修
